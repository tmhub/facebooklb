<?php
    if (Mage::helper('core')->isModuleOutputEnabled('TM_FacebookLB')) :
        $product = Mage::registry('current_product');
        $buttonHtml = $this->helper('facebooklb')
            ->renderLikeButton($product->getProductUrl(), 'productlike');
        echo $buttonHtml;
   endif;
?>
